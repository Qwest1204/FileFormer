## to run dataset preparation run this comma:

```bash
python create_dataset.py source_folder out_root -b block_size:int
```

important now avail onli PNG/JPEG/MP4/OBJ/ZIP, other format can work incorrect